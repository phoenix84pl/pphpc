#!/usr/bin/env php
<?php

$autoloadPath = getcwd() . '/vendor/autoload.php';

if (!file_exists($autoloadPath)) {
    echo "❌ Błąd: Uruchom skrypt z katalogu głównego aplikacji.\n";
    exit(1);
}

require_once $autoloadPath;

\Phoenix\Core\Console\UpdateDatabaseViews::run(getcwd());