#!/usr/bin/env php
<?php

$baseDir = getcwd();
require_once $baseDir . '/vendor/autoload.php';

// 1. Ładujemy ten sam sprawdzony Bootstrap co na WWW ($db + .env)
\Phoenix\Core\Bootstrap::init($baseDir);

// 2. Uruchamiamy aktualizację widoków SQL
\Phoenix\Core\Console\UpdateDatabaseViews::run($baseDir);